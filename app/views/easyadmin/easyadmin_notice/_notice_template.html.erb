<div class="row">
	<div class="col-xs-2">
		<img src="<%= get_notice_icon(reward) %>" class="img-responsive notice__property-icon">
	</div>
	<div class="col-xs-2">
		<img src="<%= reward.main_image.url %>" class="img-responsive notice__reward-image">
	</div>
	<% if reward.media_type && reward.media_type.downcase == "calltoaction" %>
		<div class="col-xs-8">	  
		  	<h5 class="notice__title">Complimenti! Hai sbloccato un contenuto esclusivo. <a href="/call_to_action/<%= reward.call_to_action_id %>" style="color: #fff; text-decoration: underline;">Guardalo qui</a></h5>
		</div>
	<% else %>
		<div class="col-xs-8">	  
		  	<h5 class="notice__title">Complimenti! Hai ottenuto il badge <b><%= reward.title %></b></h5>
		</div>
	<% end %>
</div>