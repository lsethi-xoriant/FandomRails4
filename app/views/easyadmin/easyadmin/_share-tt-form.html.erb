<div class="panel panel-default">
	<% if f.object.id %>
		<div class="panel-heading">&nbsp;</div>
	<% else %>
		<div class="panel-heading">&nbsp; 
			<p style="float: right"><%= link_to_remove_share_tt_fields "rimuovi" %></p>
		</div>
	<% end %>
	<div class="panel-body">
		<%= f.hidden_field :resource_type, :class => "form-control" %>
		<%= f.fields_for :resource do |shareform| %>
			<%= shareform.hidden_field :share_type, :class => "form-control", value: "twitter" %>

			<div class="col-lg-12">
				<div class="form-group">
					<label class="text-input">Messaggio</label>
				   	<%= shareform.text_field :message, :class => "form-control" %>
				</div>
				<div class="form-group">
					<label class="text-input">HashTag</label>
				   	<%= shareform.text_field :description, :class => "form-control" %>
				</div>
			</div>

		<% end %>
  	</div>
</div>