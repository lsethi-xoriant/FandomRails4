<%= render partial: "/easyadmin/easyadmin/left_bar", locals: { page: "contest" } %>
    
<div id="content">    

  <div id="content-header">
    <h1>NUOVA PERIODICITA</h1>
  </div> <!-- #content-header --> 

  <div id="content-container">

    <div class="row">
      <div class="col-md-12"> 
        <% if @periodicity.errors.any? %>
          <div class="row">
            <div class="col-lg-12">
              <div class="alert alert-danger">
                <p>Errori nel salvataggio della PERIODICITA':</p>
                <p>
                  <ul>
                    <% @level.errors.full_messages.each do |msg| %>
                      <li><%= msg %></li>
                    <% end %>
                  </ul>
                </p>
              </div>
            </div>
          </div>
        <% elsif flash[:notice] %>
          <div class="row">
            <div class="col-lg-12">
              <div class="alert alert-success"><%= flash[:notice] %></div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    
     <%= form_for @periodicity, url: "/easyadmin/periodicity/save" , html: { multipart: true } do |f| %>

      <h3>Dati generali</h3>
      <hr/>

      <div class="row">
        <div class="col-lg-6">
          <div class="form-group">
              <label class="text-input">Titolo</label>
              <%= f.text_field :name, :class => "form-control" %>
          </div>
          <div class="form-group">
              <label class="text-input">Periodicità (in giorni)</label>
              <%= f.text_field :period, :class => "form-control" %>
          </div>      
        </div>
        <div class="col-lg-6 text-center">
          
        </div>
      </div>

      <div class="row text-center">
        <div class="col-sm12">
          <button type="submit" class="btn btn-primary btn-lg">SALVA</button>
        </div>
      </div>     
    <% end %>
  </div>
</div>