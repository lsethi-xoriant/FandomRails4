<tr>

  <td><%= "#{user.first_name} #{user.last_name}" %></td>

  <td><%= user.username %></td>

  <td><%= user.email %></td>

  <td><% "#{user.location}, (#{user.province})" if user.location.present? %></td>

  <td>
    <div class="row">
      <%= get_user_buttons(user) %>
    </div>
  </td>

</tr>